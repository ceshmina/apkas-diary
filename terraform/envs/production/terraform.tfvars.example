# このファイルを terraform.tfvars にコピーして実値を埋める。
# terraform.tfvars は .gitignore によりコミットされない。

aws_region  = "ap-northeast-1"
aws_profile = "apkas-diary-production"

# production の AWS アカウント ID（12桁）。
# 間違えると staging を操作してしまうため、必ず確認してから入れる。
#   aws sts get-caller-identity --profile apkas-diary-production
aws_account_id = "000000000000"
