## 1. リポジトリの土台

- [x] 1.1 Node.js プロジェクトを初期化し、`.node-version` に 22 を記録する
- [x] 1.2 TypeScript と Lint / Format の設定を追加する
- [x] 1.3 `.gitignore` に `node_modules/`、ビルド生成物、Markdown の書き出し先ディレクトリ、Terraform のローカル生成物（`.terraform/`、`*.tfstate*`、`*.tfvars`）を追加する
- [x] 1.4 `README.md` に必要なツール（Node.js 22、Terraform 1.10 以降、AWS CLI）と AWS profile の前提を記載する
- [x] 1.5 AWS リージョンと2環境の profile 名・アカウント ID を書き込む設定ファイルの雛形を用意し、実値はコミット対象外にする

## 2. Terraform: state の bootstrap

- [x] 2.1 state 保存用 S3 バケットを作成する手順を README に記載する（バージョニング有効・パブリックアクセス全ブロック・暗号化）
- [x] 2.2 bootstrap を AWS CLI で実行するスクリプトを用意し、profile を引数で受け取る形にする
- [x] 2.3 staging と production それぞれで bootstrap を実行し、バケットの作成を確認する

## 3. Terraform: モジュール

- [x] 3.1 `terraform/modules/storage` に DynamoDB テーブルを定義する（`pk` / `sk`、オンデマンド課金、PITR 有効）
- [x] 3.2 `storage` に GSI1（`gsi1pk` / `gsi1sk`、プロジェクション ALL）を追加する
- [x] 3.3 `storage` のテーブル名を環境名から組み立て、名前から環境が判別できるようにする
- [x] 3.4 `terraform/modules/delivery` にサイト配信用 S3 バケットを定義する（パブリックアクセス全ブロック・バージョニング有効）
- [x] 3.5 `delivery` に CloudFront ディストリビューションと OAC を定義し、バケットポリシーを OAC からの読み取りのみに限定する
- [x] 3.6 `delivery` にビューアプロトコルの HTTPS リダイレクト強制と、404 用のカスタムエラーレスポンスを設定する
- [x] 3.7 各モジュールの出力（テーブル名、バケット名、ディストリビューション ID、配信ドメイン）を定義する

## 4. Terraform: 環境

- [x] 4.1 `terraform/envs/staging` に backend 設定（S3、`use_lockfile = true`）と provider を定義する
- [x] 4.2 staging の provider に `allowed_account_ids` を設定し、認証情報の取り違えを適用前に失敗させる
- [x] 4.3 staging から `storage` と `delivery` モジュールを呼び出す
- [x] 4.4 `terraform/envs/production` を同じ構成で定義する（backend のキー・アカウント ID・環境名のみ差分）
- [x] 4.5 Terraform のバージョン制約を 1.10 以降に固定する
- [x] 4.6 誤った profile で `terraform plan` を実行し、リソースを変更せずに失敗することを確認する

## 5. データアクセス層

- [x] 5.1 AWS SDK for JavaScript v3 の DynamoDB クライアントを依存に追加し、環境変数からテーブル名とリージョンを受け取る初期化処理を書く
- [x] 5.2 エントリの型定義と、DynamoDB アイテムとの相互変換を実装する
- [x] 5.3 公開状態に応じた GSI キー属性の付与・削除ロジックを実装する（`draft` のあいだは `gsi1pk` / `gsi1sk` を書かない）
- [x] 5.4 日付の検証を実装する（`YYYY-MM-DD` 形式、暦上実在する日付、`Date` 型を経由しない文字列比較）
- [x] 5.5 特定日の取得を実装する（`GetItem`）
- [x] 5.6 年別・月別の取得を実装する（`Query` と `begins_with`）
- [x] 5.7 最新N件の取得を実装する（GSI1 の降順 Query）
- [x] 5.8 公開分の全件取得を実装する（GSI1 の Query、ページング対応）
- [x] 5.9 下書きを含む全件取得を実装する（ベーステーブルの `Scan`、`type` でのフィルタ、ページング対応）

## 6. エントリ登録スクリプト

- [x] 6.1 Markdown ファイルと日付・公開状態を受け取る CLI を実装する
- [x] 6.2 `createdAt` / `updatedAt` を UTC の ISO 8601 で管理する（既存エントリの更新時は `createdAt` を保持する）
- [x] 6.3 同一日付への登録が2件目を作らず既存エントリの更新になることを確認する
- [x] 6.4 公開状態の切り替えで GSI キー属性が正しく付与・削除されることを確認する
- [x] 6.5 使い方を README に記載する

## 7. Astro サイト

- [x] 7.1 Astro をセットアップし、ビルド時にデータアクセス層を呼べることを確認する
- [x] 7.2 サイト全体のレイアウトと、日別ページへのリンクを持つエントリ一覧のコンポーネントを作る
- [x] 7.3 Markdown を HTML に整形する処理を組み込む
- [x] 7.4 トップページを実装する（最新N件。エントリ0件でもエラーにしない）
- [x] 7.5 日別ページ `/YYYY/MM/DD` を実装する（公開分のみ生成し、それ以外は 404）
- [x] 7.6 年別アーカイブ `/YYYY` を実装する
- [x] 7.7 月別アーカイブ `/YYYY/MM` を実装する
- [x] 7.8 `/on-this-day/MM-DD` を366通りすべて生成する（閏日を含む。該当なしでもページを出す）
- [x] 7.9 「今日の分」への導線を、訪問時刻から月日を解決するクライアント側スクリプトとして実装する
- [x] 7.10 404 ページを実装する
- [x] 7.11 サイト生成が GSI1 経由の取得のみを入力とし、公開状態でのフィルタ処理を含まないことを確認する

## 8. Markdown 書き出し

- [x] 8.1 下書きを含む全件を Markdown ファイルとして書き出す処理を実装する（1エントリ1ファイル、日付から一意に定まるパス）
- [x] 8.2 日付・タイトル・公開状態・本文が単体で読める形式にする
- [x] 8.3 書き出しを冪等にする（属性の順序を固定し、データ無変更なら書き出されたファイル群に差分が出ないこと）
- [x] 8.4 書き出しをサイト生成のコマンドに組み込み、単独の手動操作を不要にする
- [x] 8.5 書き出しの失敗時にサイト生成を異常終了させる
- [x] 8.6 データを変更せずに2回ビルドし、2回の書き出し結果が一致することを確認する
- [x] 8.7 書き出し先が `.gitignore` により除外され、`git status` に現れないことを確認する

## 9. デプロイ

- [x] 9.1 生成物を S3 へ反映するスクリプトを実装する（`aws s3 sync --delete`）
- [x] 9.2 CloudFront への `/*` invalidation をスクリプトに組み込む
- [x] 9.3 スクリプトが profile と環境を引数で受け取り、対象環境を明示的に指定させる形にする
- [x] 9.4 ビルドからデプロイまでの手順を README に記載する

## 10. staging での検証

- [x] 10.1 staging に Terraform を適用する
- [x] 10.2 登録スクリプトでダミーのエントリを複数年ぶん投入する（公開・下書きの両方を含める）
- [x] 10.3 ビルドとデプロイを実行し、CloudFront 経由で全種類のページが表示されることを確認する
- [x] 10.4 下書きが公開サイトのいかなる生成物にも含まれないことを確認する
- [x] 10.5 S3 のオブジェクト URL への直接アクセスが拒否されることを確認する
- [x] 10.6 HTTP でのアクセスが HTTPS にリダイレクトされることを確認する
- [x] 10.7 存在しない日付・年月・パスで 404 が返ることを確認する
- [x] 10.8 「N年前の今日」のページに複数年のエントリが年の降順で並ぶことを確認する
- [x] 10.9 エントリを削除して再デプロイし、該当ページが配信されなくなることを確認する
- [x] 10.10 書き出された Markdown に下書きを含む全件が含まれることを確認する

## 11. production への展開

- [x] 11.1 production に Terraform を適用する
- [x] 11.2 staging のリソースが変化していないことを確認する
- [x] 11.3 production の DynamoDB で PITR が有効になっていることを確認する
- [x] 11.4 エントリを投入しないままビルドとデプロイを実行し、空のサイトが配信されることを確認する
- [x] 11.5 書き出しが0件で正常終了し、`export/production/` が作られることを確認する
- [x] 11.6 運用手順（エントリの追加・公開・デプロイ）を README にまとめる
