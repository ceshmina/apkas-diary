## 1. 権限（コードより先に当てる）

手元のビルドは admin の profile で動くため、**権限の不足は手元では再現しない**。コードを先に入れると、気づくのは staging のボタンを押したときになる（design.md - Context 3）。

- [x] 1.1 `terraform/modules/publish/main.tf` に、目録のパーティションに限ったベーステーブルへの Query を足す（design.md 決定4）
  - `dynamodb:LeadingKeys` を `ForAllValues:StringLike` で `PHOTO#*` に限る。`ENTRY#<年>` は権限の側で引けないままにする
  - 既存の `QueryPublishedEntries` のコメント（「ベーステーブルへの Query は与えない」）を、**なぜ目録だけは与えるのか**が読める形に書き足す。消して書き換えるのではなく、条件付きで開いたことが分かるように残す
- [x] 1.2 staging に terraform apply して、増えたのが statement 1つだけであることを plan の差分で確かめる
  - `Plan: 0 to add, 1 to change, 0 to destroy`。増えたのは `QueryPhotoCatalog` の1つで、`dynamodb:LeadingKeys` が `PHOTO#*` に効いていることを plan の出力で確かめた
  - この時点では誰も使わない権限が1つ増えるだけで、既存の公開手続きの動きは変わらない

## 2. 配信パスの規約（`src/lib/photo.ts`）

本文が知っているのは配信 URL だけで、そこから元写真のキーは**復元できない**（拡張子が落ちている）。前向きに組み立てたものどうしを突き合わせる（design.md 決定1）。

- [x] 2.1 `photoPathOf(sourceKey)` を足す。配信キーからサイズ名を除いた部分（`2026/08/13/DSCF1234.webp`）を返す
  - `photoKeyOf` を `${size}/${photoPathOf(sourceKey)}` の形に書き直す。**規約が2箇所に分かれないようにする**。作る文字列は変わらない
- [x] 2.2 `photoPathFromUrl(base, url)` を足す。配信 URL から配信パスを取り出し、基点が一致しないもの・段数が足りないものは `undefined` を返す
  - 各段は `decodeURIComponent` で戻す（`photoUrlOf` が段ごとに符号化しているため）。空白を含むファイル名で往復することを確かめる
  - **URL から `sourceKey` を逆に導く関数は作らない。** 導けない（`.jpg` / `.JPG` / `.HEIC` のいずれからも同じ URL になる）ことと、推測で埋めると外れた写真だけ静かに機材が出なくなることを、コメントに残す
- [x] 2.3 配信パスから日付を取り出す手段を用意する。`photoSourceOf` と同じ規約（`YYYY/MM/DD/<ファイル名>`、暦上実在する日付）で判定する
  - `photoDateOf` は `photoSourceOf` をそのまま通す。配信パスは元写真のキーと同じ形をしているので、規約は1つのままでよい
- [x] 2.4 `npm run check` を通す
  - `astro check`（公開サイト・編集アプリケーションの両方）・`biome`・lambda の型検査はすべて通る
  - **`npm run check` そのものは手元では通らない。** biome が gitignore 対象の `migration/` を見ており、写真の移行が残した snapshot の JSON 731件が末尾改行なしで引っかかる。この change の前から同じで、触った範囲には1件も無い

## 3. 機材の字面（`src/lib/equipment.ts`）

- [x] 3.1 目録の `PhotoExif` から、拡大表示に出す1行を組み立てる関数を作る（design.md 決定5）
  - 表は design.md 決定5。`model` のみ / `model, lensModel` / `lensModel` のみ / なし の4通り
  - **`make` は使わない。** EXIF の `Make` はメーカーの書いた字面がそのまま入っており、大文字の揺れを画面に持ち込むこと、`Canon EOS R6` のように `Model` が既にメーカー名を含む例があることを理由としてコメントに残す
  - レンズを出すかどうかは **`lensModel` が `model` を含むかどうか**だけで決める。大文字小文字は無視し、前後の空白は落として比べる
  - **メーカーや機種の一覧を持たない。** 一覧に載っていない端末が来た日に長い文字列がそのまま出ること、気づくのはその写真を拡大したときであることを、採らなかった理由として書き残す
  - どちらも記録がなければ `undefined` を返す。空文字を返さない（不在は不在として表す）
- [x] 3.2 手元の値で4通りを確かめる。少なくとも iPhone（`LensModel` が機種名で始まる）と、レンズを交換する機材の1つを実データから取って確かめる
  - staging の目録の写真74件から機材の組み合わせを数え、実在する16通りすべてを通した。4通りの分岐がすべて現れている
  - **EXIF の文字列が NUL で終端されている機種が実在した**（`RICOH GR IIIx\u0000`）。`trim()` は NUL を落とさないので、制御文字を先に外す処理を `textOf` に足した。これが無ければ NUL がそのままページに載っていた
  - **`lensModel` が `model` を含むかだけでは分けられないことが、production の 2,416枚で分かった。** `DSC-RX1RM3`（レンズを選べない）の `35.0 mm f/2.0` と、`NIKON D5600`（レンズを交換する）の `18.0-55.0 mm f/3.5-5.6` は同じ形で、望ましい結果が逆になる。**レンズを出さない機種の一覧**を持つ形に変えた（design.md 決定5 を書き直し、spec も直した）
  - 併せて、レンズの接点を持たない機材の `----`（8枚）を落とす判定を足した。「読めなかった」を書き写した値であって、レンズの名前ではない
  - **production の目録 2,416枚は、全枚数が `exif` を持っていた**（`exif なし: 0`）。旧サイト由来のものも含めて記録の無い写真は1枚も無く、全枚数から機材の1行が出る（59通り）。design.md の Open Questions の1つ目はこれで閉じた
- [x] 3.3 `npm run check` を通す
  - 2.4 と同じ（`migration/` を除いてすべて通る）

## 4. 目録の取得（`src/lib/site-data.ts`）

- [x] 4.1 `src/migrate/parallel.ts` を `src/lib/parallel.ts` へ移し、移行の側はそこを読むようにする（design.md 決定2）
  - 中身は変えない。**`src/migrate/` は移行が済めば消える前提のディレクトリ**であり、公開サイトの生成がそこに依存してはいけないことを、移した理由としてコメントに残す
- [x] 4.2 配信パス → 機材の字面の Map を作る関数を `site-data.ts` に足す
  - **返すのは Map ではなく引き当ての関数にした。** 整形（`markdown.ts`）が持っているのは `src` だけで、そこから何を鍵に引くかは写真の URL の規約の話である。関数にすれば、規約を知るのは `site-data.ts` と `photo.ts` の2つに閉じる
  - 公開済みエントリの本文から配信の基点で始まる URL を拾い、配信パスにし、**日付で重複を畳んでから** `listPhotosByDate()` を引く
  - Promise を memo 化して、全ページが同じ Map を見る（`publishedEntries()` と同じ流儀）
  - 同時に走らせる本数に上限を置く
  - **日付をエントリの日付ではなく URL から取る**理由（本文が別の日の写真を指しうる。一致を前提にすると、一致しなくなった日に静かに出なくなる）をコメントに残す
  - **Scan を使わない**理由（`queries.ts` の「走査は公開サイトの生成の経路には現れない」「写真が増えるほどサイトの生成が重くならない」）を書き残す
- [x] 4.3 冒頭の「このモジュールが、公開サイトのビルドにおける唯一のデータ入力である」を書き換える（design.md 決定3）
  - 入力が2つになったこと、**それでも下書きが漏れうる経路は1本のまま**であること（エントリの取得は `listAllPublished()` だけ・写真は公開状態を持たない・引く日付は公開済みの本文に現れたものだけ）を、保証の形として書く
- [x] 4.4 `scripts/build.sh` の `require_env_vars` に `PHOTO_URL` を足す。欠けたときに何をどこに転記すればよいかが出ることを確かめる
  - 手元では `config/<環境>.env` を、CodeBuild では `terraform/modules/publish` を案内する既存の分岐がそのまま効く（実際に空にして確かめた）
- [x] 4.5 `npm run check` を通す
  - 2.4 と同じ（`migration/` を除いてすべて通る）

## 5. 本文への載せ方（`src/lib/markdown.ts`）

- [x] 5.1 `renderMarkdown` が引き当ての関数を**任意で**受け取れるようにする（design.md 決定6）
  - processor はプラグインを作るときに決まるので、**引き当ての関数ごとに1つ持つ**形にした（`WeakMap`）。ビルドが渡すのは memo 化された1つの関数なので、実際に作られるのは渡す側と渡さない側の2つだけ
  - 渡さない呼び出し（編集アプリケーションのプレビュー2箇所）の出力がいまと変わらないことを確かめる
- [x] 5.2 hast の `img` に `data-equipment` を付けるプラグインを足す
  - 既存の `imageFigurePlugin` が `img` の properties を丸ごと写して `figure` を組み立てているので、**`figure` に組み替えられた写真でも属性が残る**ことを確かめる。順序に依存するなら1つのプラグインにまとめる
  - 記録のない写真には**属性を付けない**。空の属性を付けると、拡大表示の側で「空文字か未設定か」を見分ける必要が生まれる
  - 処理系が `data-` 属性をどの形（`data-equipment` / `dataEquipment`）で受けるかを実際の出力で確かめる
  - **木のノードは読み取り専用だった。** 属性を足すときは同じ `img` に差し替える形にし、説明の組み替えと1つの visit にまとめた（分けると差し替えの前後どちらで載せるかが並び順に依存する）
  - **生の HTML の `<img>` は木の `img` にならない。** 旧サイトから引き継いだ記事は画像を横に並べるために `table` を本文に直接書いており、ここを外すと過去の写真の大半に機材が出ない。`raw` の visitor で、その1タグだけに属性を足した（生の HTML を解釈して木に変えると、素通りしていた記事の出力がこの change のついでに変わりうる）
  - **本文（DynamoDB の値）は書き換えない**ことを、`markdown.ts` の既定の流儀としてコメントに残す
- [x] 5.3 `src/pages/[year]/[month]/[day].astro` が引き当てを渡すようにする
- [x] 5.4 `npm run check` を通す
  - 説明つき・説明なし・段落の途中・生の HTML の table・記録のない写真・リンクの中、の6通りで出力を確かめた。**引き当てを渡さない場合の出力は、この変更の前と1文字も違わない**

## 6. 拡大表示（`src/components/`）

- [x] 6.1 `PhotoZoom.astro` の `figure` に、`figcaption` の下へ機材の行を足す（design.md 決定7）
  - `:empty { display: none }` で、記録のない写真では要素ごと消す（説明と同じ手）
- [x] 6.2 体裁を整える。**新しい色も新しい大きさも増やさない**
  - 書体は `--font-ui`（欧文のラベル）。説明は本文系の字のままにし、**書体の違いが著者の言葉と機械の記録の違いに重なる**ことをコメントに残す
  - 色は白の不透明度 0.48（説明は 0.72）。暗幕の上での contrast が 4.5 を上回ることを書き残す
  - 大きさは前後の導線の日付と同じ 0.74rem・letter-spacing 0.08em
- [x] 6.3 `photo-zoom.ts` の `Cell` と `Photo` に機材を足し、`fill()` が説明と同じように入れ替えるようにする
  - `wrap()` で `img.dataset` から読む。`captionOf()` が `figure` から説明を読んでいるのと同じ手つきにする
  - **`close()` で説明を消しているのと同じ扱いを機材にも与える**（`opacity = '0'`）。写真が本文へ帰るとき、機材の行だけが残ってはいけない
  - `place()` が説明の `opacity` を戻しているのと同じ場所で、機材も戻す
  - `aria-label` には入れない（いまの `dialog` の `aria-label` は「それが何の写真か」を言う言葉である）
- [x] 6.4 `npm run check` を通す
  - `astro check`（公開サイト・編集アプリケーション）・`biome`（`migration/` を除く）・lambda の型検査・`terraform fmt` すべて通る

## 7. 手元での確認（staging のデータ）

- [x] 7.1 staging のデータからビルドし、生成物の `data-equipment` を数える
  - 写真を含むページ数・`img` の数・属性の付いた `img` の数を出す。**付かなかったものの割合**を記録する（design.md - Open Questions の1つ目）
  - 409ページ中、写真を含むのは13ページ。写真の `img` は60、**属性が付いたのは60（100%）で、付かなかったものは0**
  - 一覧を持つ形に直したあとも同じ（`DSC-RX1RM3` の13枚が機種名だけになった）
  - 目録には exif を持たない写真が7件あるが、いずれも公開済みの本文からは参照されていない
- [x] 7.2 スマートフォンで撮った写真とレンズを交換する機材の写真の両方について、出ている字面を目で見て確かめる
  - 生成物に出ている11通りを数え上げた。`iPhone 15 Pro`（11枚、レンズは出ていない）・`ILCE-7RM5, FE 24-70mm F2.8 GM II`（19枚）・`RICOH GR IIIx, GR LENS 26mm F2.8`（NUL が落ちている）など
  - 生の HTML の table の中の `img`（旧サイト由来）にも、Markdown の `figure` の `img` にも属性が付いていることを、生成された HTML で確かめた
  - **描画そのものの目視はこの環境ではできない**（ブラウザが無い）。8.3 に残す
- [x] 7.3 ビルドにかかる時間を、この変更の前後で比べて記録する（design.md - Risks の2つ目）
  - staging（エントリ23件・写真のある日13）で、増えたのは **108 ms**（同じ経路のエントリの取得は 286 ms）。ビルド全体は 2.81 秒
  - **staging は小さすぎて目安にならない。** 往復は写真のある日の数で増え、8本ずつ束ねて走る。production は写真のある日が数百あるので、9.3 で実測する
- [x] 7.4 配信物に、位置情報・撮影日時・露出のいずれも現れないことを確かめる（spec「配信物に現れるのは機材の名称に限る」）
  - 生成物にある `data-` 属性は `data-equipment` の60件だけ。座標・EXIF・露出・撮影日時を指す語は1ページにも現れない
- [x] 7.5 編集アプリケーションのプレビューの出力が、この変更の前後で変わらないことを確かめる
  - 書き出した staging の本文25件を、変更前の `renderMarkdown` と変更後（引き当てを渡さない呼び出し）の両方に通した。**差分は0件**

## 8. staging での確認

- [x] 8.1 コードを staging に反映する
  - 409ページを生成して同期し、CloudFront を無効化（I99WVCSG2U1LEF1R5LK6XFA3S6）
  - 配信されているものを確かめた。`/2024/08/30/` に `iPhone 15 Pro` が2つ、`/2024/09/29/` に `ILCE-7RM5, FE 24-70mm F2.8 GM II` が15と `…FE 70-200mm F2.8 GM OSS II` が4
- [x] 8.2 権限が足りていることを確かめる（手元のビルドは admin の profile で動くので、そのままでは確かめられない）
  - **ボタンからの起動はここではできない。** CodeBuild の `source_version` は `refs/heads/main` 固定で（`source_branch` の既定、staging でも上書きしていない）、**いま押すと `main` の内容——この change の入っていないサイト——が配信される**。`site-publishing` の「公開手続きが用いるサイトの定義は共有された版に由来する」がそのとおり効いている
  - 代わりに IAM のポリシーシミュレータで、実行ロールそのものを評価した。ボタンを押さずに、権限だけを確かめられる

    | 主体 | 操作 | 対象 | 結果 |
    | --- | --- | --- | --- |
    | `apkas-diary-publish-staging` | `Query` | ベーステーブル + `LeadingKeys = PHOTO#2025-06-26` | **allowed** |
    | 同上 | `Query` | ベーステーブル + `LeadingKeys = ENTRY#2025` | **implicitDeny** |
    | 同上 | `Query` | GSI1 | allowed（従来どおり） |
    | 同上 | `Scan` | ベーステーブル | allowed（従来どおり） |
    | 同上 | `PutItem` / `UpdateItem` / `DeleteItem` | ベーステーブル | implicitDeny（従来どおり） |

  - **エントリのパーティションは権限の側で引けないまま**であることが、設計の意図どおり確かめられた
- [x] 8.3 staging のサイトで目視する
  - 機材のある写真・ない写真・スマートフォンの写真・説明と併せて出る写真の4通り
  - 説明のない写真で、機材が説明の位置を占めていないこと
  - 他の写真へ移ったときに機材が入れ替わること、記録のない写真へ移ったときに行が消えること
  - 閉じたときに機材の行が残らないこと、本文には機材が出ていないこと
  - **書き手が目視した**（この環境にはブラウザが無い）。生成物と配信されている中身は機械で確かめてある
- [x] 8.4 スクリプトを止めた状態でページを表示し、姿がこの変更の前と変わらないことを確かめる
  - **書き手が目視した**（この環境にはブラウザが無い）

## 9. production への適用

反映は手元から行う。**ボタンが配るのは `main` の最新であり**（`source_version = refs/heads/main`）、この change が main に入るまでは押しても変更前のサイトが配られる。ボタンからの通しの確認は、取り込んだあとに書き手が行う（この change のタスクからは外す）。

- [x] 9.1 production に terraform apply する（コードより先）
  - `Plan: 0 to add, 1 to change, 0 to destroy`。staging と同じ `QueryPhotoCatalog` 1つ。適用後の plan は差分なし
  - ポリシーシミュレータで確かめた。`PHOTO#2025-06-26` は allowed、`ENTRY#2025` は implicitDeny
- [x] 9.2 コードを production に反映し、生成物の `data-equipment` の数を記録する
  - 1,139件から 1,556ページ。**写真を含むのは739ページ、写真の `img` は2,415、機材が付いたのは2,415（100%）で、付かなかったものは0**
  - 字面は57通り。生成物にある `data-` 属性は `data-equipment` の2,415件だけ
  - 目録の取得は **1,249 ms**（写真のある日 740、8本ずつ）。同じ経路のエントリの取得が 416 ms で、ビルド全体は 6.98 秒。**写真のある日の数で増えるという読みどおりで、枚数では増えない**
  - 同期して CloudFront を無効化（I9J1SMNVLYRJ7C0D2EXKZDS2Y1）
- [x] 9.3 過去の日記を何日か開いて目視する。**移行した写真（旧サイト由来）と、新基盤で投入した写真の両方**を含む日を選ぶ
  - 配信されている中身は確かめた。`/2024/03/05/`（`figure` の説明13個 + 3機材）・`/2024/09/06/`（旧サイト由来の table + 4機材）・`/2025/09/18/`（`DSC-RX1RM3` 13枚）
  - **書き手が目視した**（この環境にはブラウザが無い）

## 10. 記録

- [x] 10.1 README に、拡大表示に機材が出ること・出る条件（記録があること、レンズは機種名を含まないとき）を書く
  - 併せて2箇所を直した。「公開サイトの生成は写真が何枚増えても変わらない」（目録を読むようになったので、増え方を正確に書き直した）と、公開手続きの権限の説明（DynamoDB の権限が2つから3つになった）
- [x] 10.2 数えた結果（機材を持つ写真の割合、ビルド時間の変化）を change 内に短く残す
  - `report.md` に残した

## 11. レンズを出さない機種の一覧を広げる（仕様の追加）

書き手からの追加。**RICOH GR 系もコンパクト機なのでレンズを出さない。** 併せて、他にコンパクト機・スマートフォンが無いかを目録で確かめる。

- [x] 11.1 production の目録にある機種をすべて分類する
  - 16機種。レンズを交換する7つ（`ILCE-7M3` / `ILCE-7RM5` / `ILCE-7S` / `ILCE-7CM2` / `E-P7` / `DC-GF9` / `NIKON D5600`）と、コンパクト機・スマートフォン8つに分かれた
  - `foodie`（52枚、`Make` は `Snowcorp`）は写真のアプリが機種名の位置に自分の名前を書いたもの。機材の系列ではなく、レンズの記録も持たない
- [x] 11.2 一覧を**機種ではなく系列**で持つ形にし、見つかったものを足す（design.md 決定5）
  - 足したのは `RICOH GR`（280枚。`GR LENS 18.3mm F2.8` / `GR LENS 26mm F2.8` が落ちる）と `LEICA D-Lux`（21枚。`DC VARIO-SUMMILUX …` が落ちる）
  - レンズの名称を記録しない `XQ-`（Xperia）と `X100` も足した。**いまは何も変わらないが、字面の規則ではなく機材の分類である**
  - `DSC-RX1RM3` は `DSC-`（Cyber-shot）に広げた。系列はメーカーが交換式と分けて付けている（交換式は `ILCE-`）
- [x] 11.3 production の 2,416枚に当てて、レンズを交換する機材のレンズ名が落ちていないことを確かめる
  - 59通り → 57通り。減ったのは `RICOH GR III` / `RICOH GR IIIx` / `LEICA D-Lux 8` がレンズ名ごとに分かれていたぶんが畳まれたため
  - `NIKON D5600` の3本・`E-P7` の3本・`DC-GF9` の2本・`ILCE-*` のすべては**そのまま残っている**
- [x] 11.4 staging を作り直して反映する
  - 60枚すべてに属性が付いたままで、`RICOH GR III` / `RICOH GR IIIx` が機種名だけになった。CloudFront を無効化（I5WMWN6F4610F627JG2YXRQ9CH）
