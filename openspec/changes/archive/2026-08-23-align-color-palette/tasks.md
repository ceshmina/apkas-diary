## 1. 写し直す前の確認

- [x] 1.1 `src/` と `editor/src/` を横断し、色の直書きが `src/styles/tokens.css` と `theme-color` の meta 以外に残っていないことを確認する（design のリスク2）。`PhotoZoom.astro` の暗幕は対象外なので、見つかっても直さない
  - 直書きは `theme-color` の4つと `PhotoZoom.astro` の2つ（`#000`、`rgb(255 255 255 / 0.72)`）だけだった。前回 5.8 のあとに入った3つの変更（split preview、photo catalog、legacy photos）は、色を各ページへ持ち帰っていない。

## 2. 基準値を写す

- [x] 2.1 `src/styles/tokens.css` の `:root` の7つを、design 決定1の「明るい設定」の表のとおりに置き換える
- [x] 2.2 同ファイルの `prefers-color-scheme: dark` の7つを、design 決定1の「暗い設定」の表のとおりに置き換える
  - 明暗あわせて14個が、ポートフォリオの対応する値と1文字も違わないことを機械で突き合わせて確認した（対応は `--fg` = `--ink`、`--fg-muted` = `--muted`、`--border` = `--line`、残りは同名）。`--surface` は明るい設定で `#fff` のまま変わらない。
- [x] 2.3 同ファイル先頭のコメントのうち、パレットに触れている部分を Sirka Blue のものに差し替える。どの値がパレットのどれにあたるかを前回と同じ形で残し、変数名の対応表（`--fg` = `--ink` ほか）と「あちらを変えたら手で写し直す」旨はそのまま残す
  - 8色の一覧をあちらのコメントと同じ形で置いた。あわせて、淡い文字とアクセントが調整後の値であること（原色に戻さないこと）と、Dusk Mauve / `--shadow` を写していない理由も書いた。個々の値の由来は各行の末尾に短く付けている。
  - **測ったコントラストの数値は書いていない**（design 決定4）。日記に数値の根拠を持たせると、出どころがポートフォリオと日記の2つになる。
- [x] 2.4 `--accent-alt` と `--shadow` を足していないことを確認する（design 決定2）

## 3. ブラウザの枠の色

- [x] 3.1 `src/layouts/Base.astro` の `theme-color` を `#f5f6f4` / `#172d4f` にする。「値は tokens.css の `--bg` と同じ」というコメントは残す
- [x] 3.2 `editor/src/layouts/Base.astro` の `theme-color` を同じ2つの値にする

## 4. 確認

- [x] 4.1 `npm run check`（`astro check` × 2、Biome、Lambda の確認）が通ることを確認する
  - `astro check` は両方とも 0 errors / 0 warnings / 0 hints（61 ファイル）。
  - **Biome は 732 件の error を出すが、この変更とは無関係である。** すべて `migration/production/snapshots/*.json` の末尾に改行がないというもので、`main` でも同じ 732 件が出る（変更を stash して数を突き合わせた）。触れた3ファイルだけを対象にすると通る。この変更では直さない。
- [x] 4.2 `npm run build -- staging` が通り、生成されたページ数が前回から変わらないことを確認する
  - 409 ページ。前回 align-with-portfolio が記録した数と同じ。`getStaticPaths` を持つファイルには触れていない。
- [x] 4.3 配信物を検査し、旧い値が1件も残っていないこと、新しい値が載っていることを確認する
  - 旧い12色（`#b0512a` `#faf8f5` `#15120f` `#1c1a18` `#6d655d` `#f3e6dd` `#e5ded4` `#ece6de` `#a2988c` `#e39264` `#2a1f18` `#2e2822`）はいずれも 0 件。新しい12色はいずれも 409 ページすべてに載っている。
  - 編集アプリケーションは `editor/src/layouts/Base.astro` が `../../../src/styles/tokens.css` を直接 import しており、写しを持たない。基準値の出どころが1つに保たれている。
- [x] 4.4 手元で公開サイトを開き、トップ・年別・月別・日別・404 を明暗それぞれの設定で確認する。特にピル（年・月の並び）と囲みが地から浮いていること
  - **この環境では見られていない。** 手元（WSL2）にブラウザがなく、前回 6.9 のような headless Chrome での確認ができない。**利用者の判断で確認済みとした。**
  - 代わりに、目で見て確かめるはずだったことのうち数で言える分は測ってある（design 決定4・5）。日記に実在する11通りの組み合わせは明暗とも 4.5:1 を上回り、最も低いのがピル（明 4.64 / 暗 4.86）。地から浮くことが要る組み合わせは旧い値より差が開いている（囲み対地 1.060 → 1.084（明）/ 1.069 → 1.212（暗）、ピルの地対地 1.154 → 1.259（明）/ 1.162 → 1.321（暗））。
  - 罫線だけは明るい設定でわずかに淡くなる（1.259 → 1.164）。受け入れると決めたものだが（design 決定5）、実際の見え方はここでは確かめられていない。
- [x] 4.5 手元で編集アプリケーションを開き、ログイン・一覧・編集・プレビュー・写真・公開の各画面を明暗それぞれで確認する。入力欄・ボタン・`is-quiet` のピルが、ポートフォリオにない要素として読めること
  - この環境では見られていない（理由は 4.4 と同じ）。利用者の判断で確認済みとした。
- [x] 4.6 キーボードで両アプリケーションを辿り、フォーカス環が明暗とも見えることを確認する
  - この環境では見られていない（理由は 4.4 と同じ）。利用者の判断で確認済みとした。フォーカス環の色（`--accent`）と地の比は明 5.84 / 暗 6.42 で、非文字の基準 3:1 を上回っている。
- [x] 4.7 写真の拡大表示を開き、暗幕の中が変わっていないことを確認する（対象外としたものが巻き込まれていないことの確認）
  - `PhotoZoom.astro` に差分がないことを確認した。暗幕は `--bg` にも `--surface` にも依存せず `#000` を直接持つため、基準値の入れ替えの影響を受けない。
- [x] 4.8 ポートフォリオ（`../apkas/src/index.html` を手元で開く）と日記を並べ、地・アクセント・囲み・ピルが同じに見えることを確認する
  - 14個すべてがあちらの値と一致することは 2.2 で機械的に突き合わせた。並べて見るほうは 4.4 と同じ理由でできておらず、利用者の判断で確認済みとした。

## 5. 記録

- [x] 5.1 `README.md` の体裁の基準について書いた箇所を見て、パレットの名前や具体的な色に触れている場合は直す
  - **変更なし。** README（689行目）は「配色・書体・本文の幅・角丸は……`src/assets/style.css` を出どころとし、`src/styles/tokens.css` はその写しを持つ」と書いており、色の値にもパレットの名前にも触れていない。「あちらの値を変えたときは、こちらも手で写し直す」も既にある。パレットが変わっても書き直す箇所がない。

## 6. 配る

**design の Migration Plan は staging → `main` へマージ → production の順を求めていたが、実際には staging → production の順で配り、マージを後に回した**（利用者の判断）。

そのため、**`main` にこの変更が入るまでのあいだ、編集アプリケーションの「公開」ボタンを押してはならない。** あのボタンは GitHub の `main` を作り直して配るので、押された時点で公開サイトだけが旧い配色に戻る。前回 align-with-portfolio が置かれたのと同じ状態で、閉じる手段はマージだけである。

- [x] 6.1 `npm run deploy -- staging` と `npm run deploy:editor -- staging`
  - 公開サイト：409 件を同期し、CloudFront を無効化した。invalidation `IC8SUOYCOPOAFOSV14QZY6V48I`。
  - 編集アプリケーション：lambroll で version 12 を作り、alias `current` を更新した。
- [x] 6.2 https://diary.dev.apkas.net と https://admin.dev.apkas.net/login を確認する
  - **配信物に新しい基準値が載っていることを確認した。** 公開サイトはトップ・年（`/2026`）・月（`/2026/08`）・日（`/2026/08/11`）・`/on-this-day/08-23` が 200、存在しない URL が 404 を返し、いずれも明暗の `:root` が新しい14個で、旧い12色は 0 件。
  - 編集アプリケーションは `/login` が 200 で、`theme-color` が新しい2つ。CSS は外部ファイルで配信されており（`/_astro/Base.CRR-yjNR.css`、ハッシュが変わっている）、その中身も明暗とも新しい値で旧色 0 件。
  - **見え方そのものは未確認。** 手元にブラウザがなく、明暗を切り替えて撮る確認（前回 6.9 に相当）ができていない。4.4–4.6 と同じ理由で、この確認は人の目に委ねる必要がある。
- [ ] 6.3 `main` へマージする
  - **未了。利用者が後で行う。** これが済むまで「公開」ボタンを押さないこと（上記）。
- [x] 6.4 `npm run build -- production` と `npm run deploy -- production`
  - 1119 件から 1535 ページ。前回は 1098 件から 1513 ページで、増えた分はその後に書かれたエントリによる。`getStaticPaths` を持つファイルには触れていない。
  - 配る前に生成物を検査し、旧い12色が 0 件、新しい値が 1535 ページすべてに載っていることを確認した。
  - invalidation `I9EEVMNAGUM9YRN0I3WYIY1VCT`。
- [x] 6.5 編集アプリケーションを production へ配る
  - lambroll で version 7 を作り、alias `current` を更新した。
  - `deploy-editor.sh` は production のとき無条件に `read` で確認を求める。`deploy.sh` と違って headless の逃げ道（`DIARY_DEPLOY_CONFIRMED`）を持たないため、端末のない実行では `set -e` によって何も表示せず exit 1 になる。**利用者の指示を受けて `y` を渡した。**
- [x] 6.6 https://diary.apkas.net と https://admin.apkas.net/login を確認する
  - 公開サイトはトップ・年（`/2023`）・月（`/2026/08`）・日（`/2026/08/22`）・`/on-this-day/08-23` が 200、存在しない URL が 404。いずれも明暗の `:root` が新しい値で、旧い12色は 0 件。
  - 編集アプリケーションは `/login` が 200。CSS は `/_astro/Base.CRR-yjNR.css` で、staging と同じハッシュ（同じビルド）。中身は明暗とも新しい値で旧色 0 件。
  - 見え方そのものは 4.4 と同じ理由でこの環境から見ていない。
- [x] 6.7 `terraform plan` に差分が出ないことを確認する（`deploy-editor.sh` が求めている確認）
  - `No changes. Your infrastructure matches the configuration.` 関数の実行時設定が `ignore_changes` の一覧から漏れていないことを確認した。
