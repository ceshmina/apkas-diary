# 取り込みの結果

2026-09-23 に実施。

## 数

| | staging | production |
|---|---|---|
| 走査した記事（旧サイト） | 1,090 | 1,090 |
| 場所を持つ記事 | 107 | 107 |
| 取り込んだ件数 | 7 | **107** |
| 地点の種類 | 4 | **33** |
| 本文が変わった件数 | **0** | **0** |
| タイトル・公開状態・`createdAt` が変わった件数 | **0** | **0** |
| `updatedAt` が変わった件数 | 8 | 107 |

staging が7件なのは、代表16件しか持たないためで、取りこぼしではない。production は旧サイトが場所を書いていた107件すべてが入った。

staging の `updatedAt` が1件多いのは、CLI の確認（7.3）で 2026-08-13 の下書きに触れたため。その日の場所は確認のあと外してある。

## 表記の揺れ

**無かった。** 33地点はすべて `<都市>, <国>` の形で、同じ場所の別の書き方も、前後の空白も無い。内訳は Osaka 46、Fukushima 6、Prague 5、Beijing 4、Helsinki 4、Morocco の各地が計10、ほか国内外の都市が1〜3件ずつ。

正規化の仕組みは入れていない（proposal.md - スコープ外）。書き手は1人で、実際に揺れが無かったため、いま縛る理由が無い。

## 公開サイト

production は1,138件から1,555ページ。場所が出るのは107ページで、取り込んだ件数と一致する。`/2024/06/29/`（Kyoto, Japan）、`/2024/02/29/`（Marrakech, Morocco）、`/2025/12/24/`（Prague, Czechia）に出ており、場所を持たない `/2023/01/01/` と `/2026/06/07/` には何も出ていない。

## 書き出し

production の `export/` の差分は107ファイル。frontmatter で変わったのは `location` と `updatedAt` の2つだけで、**本文の差分は0件**。場所を持たない1,031件のファイルはこの変更の前後で同一である。

## 残った未移行はこれで無い

旧サイト（eskarun）の記事1,089件について、本文・タイトル・公開状態は移行済みであることを確認済みで、今回 `location` が入った。**旧サイトの frontmatter にある属性は、これですべて日記の側にある。**

旧サイトの写真ホスト（`photos.old.apkas.net`）の廃止は別の話として残っている（`migrate-legacy-photos` の report.md「やり残し」）。

## 途中で分かったこと

**staging では絞り込みなしに取り込めない。** 107件の日付のうち100件は staging にエントリが無く、場所だけの更新は新規作成にならないため、ガードが1件も書かずに止めた。`--only` で staging に在る7日に絞って通した。止まり方は設計どおりで、直すところは無い。

**日別ページの CSS がページ内に埋め込まれなくなった。** 規則が増えて Astro の 4KB のしきい値を越え、`/_astro/_day_.*.css` への参照に変わった。読むのは日別ページだけで、1ページあたりの HTML は約4KB小さくなる。production の同期は 431.8 MiB。
