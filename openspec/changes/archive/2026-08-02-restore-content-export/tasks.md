## 1. .gitignore の是正

- [x] 1.1 `.gitignore` の `export/` を `/export/` に変える
- [x] 1.2 他に同じ危うさを持つパターンがないか確認する（`dist/` `node_modules/` `.astro/` は同名のディレクトリがどこにあっても除外したいものなので、そのままでよい）
- [x] 1.3 変更後に `git check-ignore -v src/export/markdown.ts` が一致しないこと、`git check-ignore -v export/staging/2026/2026-08-02.md` が一致することを確認する

## 2. 書き出しモジュールの実装

- [x] 2.1 `src/export/markdown.ts` を作り、`exportEntries(): Promise<{ dir: string; written: number; removed: number }>` を公開する
- [x] 2.2 エントリ1件を frontmatter + 本文の文字列に変換する処理を書く。属性は `date` / `status` / `title` / `createdAt` / `updatedAt` の順に固定し、`title` は `JSON.stringify()` で書く（design.md 決定1・3）
- [x] 2.3 `scanAllIncludingDrafts()` で下書きを含む全件を取得し、`<書き出し先>/<年>/<YYYY-MM-DD>.md` に書き出す。年のディレクトリは必要に応じて作る
- [x] 2.4 基底ディレクトリは、エントリが0件でも作る
- [x] 2.5 書き出し先を走査し、`<年>/<YYYY-MM-DD>.md` の形に一致するファイルのうち今回のエントリに対応しないものを削除する。一致しないファイルやディレクトリには触らない（design.md 決定5）
- [x] 2.6 空になった年のディレクトリを削除する
- [x] 2.7 例外を握りつぶさず、そのまま伝播させる

## 3. 確認

固定データを流し込むために `scanAllIncludingDrafts()` を一時的に差し替えて実行した。差し替えは確認後に元へ戻してある。

- [x] 3.1 `npm run check`（`astro check` と Biome）が通ることを確認する
  - astro check 0 errors / Biome 23ファイル clean。config のロードが通るようになったことがこの change の主目的。
- [x] 3.2 固定データで書き出しを実行し、生成されたファイルの中身が design.md 決定1 の形になっていることを確認する
- [x] 3.3 冪等性を確認する。同じデータで2回書き出し、ファイル群に差分が出ないこと
- [x] 3.4 1件だけ内容を変えて書き出し、そのファイルにだけ差分が出ることを確認する
- [x] 3.5 1件を取り除いて書き出し、対応するファイルが削除され `removed` が 1 になることを確認する
  - 空になった `2024/` のディレクトリごと消えることも確認した。
- [x] 3.6 書き出し先に命名規則に合わない無関係なファイルを置いて書き出し、削除されずに残ることを確認する
  - `2026/NOTES.txt`、`misc/keep.md`、および年のディレクトリと日付が食い違う `2026/2025-01-01.md` のいずれも残った。
- [x] 3.7 `title` に `: ` や `#`、引用符、改行を含むエントリで、書き出したファイルが YAML として読めることを確認する
  - コロン+空白・`#` 始まり・引用符始まり・改行・`{}` `[]` `&` `*`・バックスラッシュ・タブ・空文字列・`--- 区切りに見える ---` の8通りを、`yaml` パーサで元の文字列に戻ることまで確認した。本文中の `---` も壊れない。
- [x] 3.8 下書きのエントリが書き出され、`status: draft` として記録されることを確認する
- [x] 3.9 エントリ0件で書き出し、基底ディレクトリが作られることを確認する

## 4. 実データでの確認

`config/staging.env` は存在しなかったため、稼働中のリソースから値を引いて作成した（gitignore 対象でコミットされない）。staging には既存のダミーエントリが6件（公開5・下書き1）ある。

- [x] 4.1 `npm run build -- staging` が通り、`export/staging/` に書き出されることを確認する
  - 382ページを生成し、`日記本文を書き出しました: 6 件` まで到達。`export/staging/<年>/<YYYY-MM-DD>.md` に6件そろった。
- [x] 4.2 続けてもう一度ビルドし、`export/staging/` に差分が出ないことを確認する
  - 実データでも差分なし。
- [x] 4.3 下書きが書き出しに含まれ、かつ配信物には含まれないことを確認する
  - `export/staging/2025/2025-12-24.md` は `status: draft` で本文ごと保全されている。`dist/` にはタイトル・本文・目印のいずれも含まれず、`dist/2025/12/` 自体が生成されない。
- [x] 4.4 staging にデプロイし、稼働中のサイトで確認する
  - `npm run deploy -- staging` を実行（S3 の削除対象は0件）。invalidation `I4Q2RZ8F9ICHJ9U8KZTXY8B2WA` が Completed。
  - https://diary.dev.apkas.net で、トップ・日別・年別・月別・月日ページが 200、下書きの `/2025/12/24` と `/2025/12` が 404、配信物に下書きの痕跡がないことを確認した。
