## 1. 取り込み CLI

- [x] 1.1 `src/legacy/source.ts` を作る。ディレクトリを再帰的にたどり、`<YYYYMMDD>.md` から日付を決め、frontmatter（`title` / `tilte` / `status`）を読み、本文は frontmatter 直後の空行と末尾の改行だけ整えて返す
- [x] 1.2 `location` は取り込まない
- [x] 1.3 `tilte` の綴り誤りを `title` として取り込み、警告に出す
- [x] 1.4 同じ日付のファイルが複数ある場合はどちらも取り込まず警告する
- [x] 1.5 `src/cli/import-legacy.ts` と `scripts/import-legacy.sh` を作る。環境名を第1引数に取り、`--source` を必須にする
- [x] 1.6 `--only` / `--from` / `--to` で対象を絞れるようにする。`--only` に指定した日付の記事がない場合はエラーにする
- [x] 1.7 `--dry-run` で、対象の一覧と新規/更新の別、警告を表示する
- [x] 1.8 `--status` の既定を `published` とし、frontmatter の `status` があればそちらを優先する
- [x] 1.9 `package.json` に `import-legacy` を追加する

## 2. 表示: タイトルのないエントリの見出し

- [x] 2.1 `src/pages/[year]/[month]/[day].astro` で、タイトルがある場合は現状どおり日付行＋タイトル、ない場合は日付行を出さず日付を `h1` にする。書式はどちらも `formatDateJa`（曜日つき）で揃える
- [x] 2.2 `(無題)` の文言を消す。`<title>` の組み立ても同じ規則に合わせる
  - 一覧（`src/components/EntryList.astro`）にも同じ文言があったため、そちらも日付だけにした。日別ページだけ直しても、一覧に `(無題)` が残っていては意味がない。
  - 一覧でタイトルのない行は日付が唯一の手がかりになるので、大きさと桁揃えはそのままに色だけリンクに寄せた（`time:only-child`）。
- [x] 2.3 どちらの場合も日付が1度しか現れないことを確認する
  - 生成物の日別ページ全件で `<time datetime=` の出現が1回であることを確認した。

## 3. 表示: 画像に添えられた説明

- [x] 3.1 `src/lib/markdown.ts` に hast プラグインを足し、段落の中で単独に置かれた画像で `alt` が空でないものを `figure` / `figcaption` に組み替える
  - プラグインの型は `SatteriMarkdownProcessorOptions['hastPlugins']` から引いた。`satteri` は直接の依存ではないため、そこから型を import しない。
- [x] 3.2 `alt` は残す
- [x] 3.3 説明なしの画像と、文章の途中に置かれた画像が組み替えられないことを確認する
  - 5つの形（説明つき単独・説明なし単独・文中・1段落に2枚・本文中の HTML の `img`）で確認した。組み替わるのは1つ目だけ。

## 4. 表示: 画像の並び

- [x] 4.1 `src/pages/[year]/[month]/[day].astro` の `.body` 配下で、`table` に `table-layout: fixed` と `width: 100%` を与える
- [x] 4.2 セルの間隔を与える
  - `border-spacing` は表の外周にも効くため、負のマージンと `calc(100% + 2 * gap)` で打ち消し、画像の端が本文の幅と揃うようにした。
- [x] 4.3 `td[colspan]` と `figcaption` を、並び／画像に対する説明の体裁（中央寄せ・小さめ・淡い色）で揃える
- [x] 4.4 グリッドの前後の余白を、段落や `hr` と釣り合う形にする
- [x] 4.5 狭い画面幅で横方向にはみ出さないことを確認する
  - 520px 幅で3列グリッドを含むページを描画し、はみ出しがないことを確認した。

## 5. 代表記事での確認

- [x] 5.1 旧記事の全パターンを洗い出し、それを網羅する最小の代表集合を選ぶ
  - 32パターンを16件で網羅。2023-01-01 / 2023-08-02 / 2023-11-01 / 2024-02-20 / 2024-07-01 /
    2024-08-30 / 2024-08-31 / 2024-09-11 / 2024-09-29 / 2025-06-26 / 2025-09-06 / 2025-09-18 /
    2025-09-22 / 2025-11-30 / 2026-01-01 / 2026-06-07
- [x] 5.2 staging に代表16件を取り込む
- [x] 5.3 表示側の変更を入れたうえで、16件それぞれの表示を確認する（見出し・キャプション・グリッド・`hr`・見出し・リスト・引用・打ち消し・インラインコード）
  - 16件すべてのページが生成され、`blockquote` / `li` / `code` / `del` / `h2` / `hr` / `strong` が対応する
    記事で出ていることを生成物で確認した。数式1件は `$2^A$` のまま出る（想定どおり）。
  - 見た目は 2024-09-29（グリッド全種＋キャプション）、2026-01-01（タイトルなし）、2025-09-18（3×3
    グリッド・見出し・打ち消し）、トップページを描画して確認した。
- [x] 5.4 `npm run check` が通ることを確認する

## 6. staging での確認

staging に入れるのは代表16件までとする（design.md の決定7）。全件は入れない。

- [x] 6.1 代表16件が既存のテスト用エントリ6件と日付で重ならないことを確認する
- [x] 6.2 `npm run build -- staging` を通し、`export/staging/` の本文が旧ファイルと一致することを16件で突き合わせる
  - 16件すべて本文が一致。既存のテスト用エントリ5件は旧サイトに同じ日付の記事があるが、取り込んで
    いないので内容が違って当然であり、突き合わせの対象外。
- [x] 6.3 `npm run deploy -- staging` して、稼働中のサイトで表示を確認する
  - invalidation `I9L31SBQYME22V4PP8IJKVG4EF` が Completed。代表16件・トップ・年別・月日ページが
    いずれも 200。配信物に `(無題)` が0件、`figcaption` と `table-layout:fixed` が届いていること、
    タイトルなしのページの `h1` が `<time>2026年1月1日（木）</time>` になっていることを確認した。
- [x] 6.4 下書きの 2025-12-24 が 404 のままであることを確認する
  - `/2025/12/24` と `/2025/12` がどちらも 404。トップ・年別・月日ページに下書きの本文の痕跡もない。

## 7. 後片付け

- [ ] 7.1 1089件の投入先と時期を決めるまで、`src/legacy/` と取り込み CLI は残す
- [ ] 7.2 移行完了後に `src/legacy/`・`src/cli/import-legacy.ts`・`scripts/import-legacy.sh`・`package.json` の `import-legacy` を削除する
