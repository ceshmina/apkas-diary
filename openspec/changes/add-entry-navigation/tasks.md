## 1. 隣接エントリの解決を site-data に足す

- [ ] 1.1 `src/lib/site-data.ts` に、導線に出す最小限の型 `EntryLink`（`date` と `title` のみ）を定義する
- [ ] 1.2 同ファイルに、公開済みエントリと隣接する `EntryLink` の組を全件分返す関数を足す。昇順配列の添字 `i` に対し `newer` は `i + 1`、`older` は `i - 1`。端では `undefined`
- [ ] 1.3 隣接の解決が `publishedEntries()` の結果だけを入力とし、ベーステーブルを引く関数を呼ばないことを確認する

## 2. 日別ページに前後の導線を出す

- [ ] 2.1 `src/pages/[year]/[month]/[day].astro` の `getStaticPaths` で、隣接する `EntryLink` を props に渡す（本文は渡さない）
- [ ] 2.2 本文の直後、既存のリンク群の上に導線の `nav` を置く。`←` が新しい側、`→` が古い側
- [ ] 2.3 導線に日付と、あればタイトルを出す。タイトルのないエントリは日付だけを出す（一覧と同じ扱い）
- [ ] 2.4 隣接しない側は要素ごと出さない。不在を示す文言は置かない
- [ ] 2.5 片側だけのとき、残った側が向きに沿った位置に留まるスタイルを当てる（古い側は右端）
- [ ] 2.6 狭い画面幅で、日付とタイトルが折り返して収まることを確認する

## 3. 一覧の並びを新しい順に揃える

- [ ] 3.1 `src/pages/[year]/index.astro` のエントリ一覧を `byDateAsc` から `byDateDesc` に変える
- [ ] 3.2 同ページの月への導線を新しい月から並べる
- [ ] 3.3 `src/pages/[year]/[month]/index.astro` のエントリ一覧を `byDateAsc` から `byDateDesc` に変える
- [ ] 3.4 トップページ・月日ページが既に新しい順であることを確認し、手を入れない

## 4. 確認

- [ ] 4.1 `npm run build` が通ることを確認する
- [ ] 4.2 `npm run check`（astro check と Biome）が通ることを確認する
- [ ] 4.3 実データのビルド生成物で、日付の飛んだ隣接・年をまたぐ隣接・最新／最古の端を確認する
- [ ] 4.4 生成物に下書きの日付が導線として現れないことを確認する
- [ ] 4.5 年別・月別ページの一覧と月への導線が新しい順になっていることを確認する
- [ ] 4.6 staging にデプロイし、稼働中のサイトで導線を辿って確認する
