## 1. 隣接エントリの解決を site-data に足す

- [x] 1.1 `src/lib/site-data.ts` に、導線に出す最小限の型 `EntryLink`（`date` と `title` のみ）を定義する
- [x] 1.2 同ファイルに、公開済みエントリと隣接する `EntryLink` の組を全件分返す関数を足す。昇順配列の添字 `i` に対し `newer` は `i + 1`、`older` は `i - 1`。端では `undefined`
  - `entriesWithNeighbors()`。`noUncheckedIndexedAccess` が有効なため、範囲外の添字は型の上でも `undefined` になり、端の扱いに条件分岐が要らない。
- [x] 1.3 隣接の解決が `publishedEntries()` の結果だけを入力とし、ベーステーブルを引く関数を呼ばないことを確認する
  - `site-data.ts` の store からの import は `listAllPublished`（GSI1）のみ。`listByYear` / `listByMonth` / `scanAllIncludingDrafts` はこのモジュールに入っていない。

## 2. 日別ページに前後の導線を出す

- [x] 2.1 `src/pages/[year]/[month]/[day].astro` の `getStaticPaths` で、隣接する `EntryLink` を props に渡す（本文は渡さない）
- [x] 2.2 本文の直後、既存のリンク群の上に導線の `nav` を置く。`←` が新しい側、`→` が古い側
  - 区切り線は `.adjacent` の1本だけにし、`.links` からは `border-top` を外した。線が1.25rem 間隔で2本続くと、
    どちらもページ末尾の導線であることが見た目から失われるため。
- [x] 2.3 導線に日付と、あればタイトルを出す。タイトルのないエントリは日付だけを出す（一覧と同じ扱い）
  - 日付だけのときに `time:only-child` で色をリンクに寄せるのも一覧と同じ。矢印は `aria-hidden` にした。
    新旧は添えた日付を読めば分かるため、矢印を読み上げさせる必要がない。
- [x] 2.4 隣接しない側は要素ごと出さない。不在を示す文言は置かない
- [x] 2.5 片側だけのとき、残った側が向きに沿った位置に留まるスタイルを当てる（古い側は右端）
  - `.older { margin-left: auto }`。`justify-content: space-between` では要素が1つのときに左寄せになる。
- [x] 2.6 狭い画面幅で、日付とタイトルが折り返して収まることを確認する
  - 当初の `flex: 0 1 auto` + `margin-left: auto` は、狭い画面で上下に折り返らず横で潰れ合い、長いタイトルが
    細い柱になった。`flex: 1 1 14rem` に変え、古い側の右寄せを `justify-content: flex-end` に移した。
    design.md の決定6も実際の作りに合わせて書き直した。
  - 本文幅は `main` の 42rem（672px）、body の左右 padding は 1.25rem。基準幅 14rem×2 + gap 1.5rem = 472px
    を保てなくなる本文幅、すなわち画面幅 512px 未満で上下に折り返る。折り返った各行は幅いっぱいに広がり、
    新しい側は左、古い側は右のまま。320px 幅では本文 280px に対し最長のタイトル（23文字 ≈ 331px）が
    2行に折り返る。`min-width: 0` があるためはみ出さない。
  - ブラウザを使った目視ではなく、生成された CSS（`flex:14rem` として出力されている）と幅の計算による確認。
    この環境に headless browser がないため。

## 3. 一覧の並びを新しい順に揃える

- [x] 3.1 `src/pages/[year]/index.astro` のエントリ一覧を `byDateAsc` から `byDateDesc` に変える
- [x] 3.2 同ページの月への導線を新しい月から並べる
- [x] 3.3 `src/pages/[year]/[month]/index.astro` のエントリ一覧を `byDateAsc` から `byDateDesc` に変える
- [x] 3.4 トップページ・月日ページが既に新しい順であることを確認し、手を入れない
  - トップは `recentEntries()`、月日ページは `sort(byDateDesc)`、トップの年は `years()` の `.reverse()` で、
    いずれも既に新しい順。`byDateAsc` を使う場所はサイト生成側に残っていない（`queries.ts` の取得順のみ）。

## 4. 確認

- [x] 4.1 `npm run build` が通ることを確認する
  - `npm run build -- staging` で 407 ページ。書き出しも 22 件走った。
- [x] 4.2 `npm run check`（astro check と Biome）が通ることを確認する
  - astro check は 0 errors / 0 warnings / 0 hints、Biome は 25 ファイルで指摘なし。
- [x] 4.3 実データのビルド生成物で、日付の飛んだ隣接・年をまたぐ隣接・最新／最古の端を確認する
  - 日別ページ21件すべての `nav.adjacent` を抽出して確認した。
  - 日付の飛んだ隣接: `/2023/01/01` の新しい側が `/2023/08/02`（7ヶ月）、`/2024/09/29` の新しい側が
    `/2025/06/26`（9ヶ月）。存在しない日付を指す導線は0件。
  - 年をまたぐ隣接: `/2024/02/20` の古い側が `/2023/11/01`、`/2026/01/01` の古い側が `/2025/11/30`。
  - 端: 最古の `/2023/01/01` に新しい側だけ、最新の `/2026/08/01` に古い側だけ。不在を示す文言はなし。
  - タイトルは、あるものだけが導線に出ている（タイトルを持たない隣は日付だけ）。
- [x] 4.4 生成物に下書きの日付が導線として現れないことを確認する
  - staging の下書きは `2025-12-24` の1件。`/2025/11/30` の新しい側はこれを飛ばして `/2026/01/01` を指す。
  - `dist/` 全体で `2025/12/24` および `2025-12-24` を含むファイルは0件。
- [x] 4.5 年別・月別ページの一覧と月への導線が新しい順になっていることを確認する
  - `/2024` のエントリは 09/29→02/20 の降順、月への導線は 9月→8月→7月→2月。`/2024/08` は 31→30→15。
  - トップページ（エントリ・年への導線）と月日ページは元から降順のまま。
- [x] 4.6 staging にデプロイし、稼働中のサイトで導線を辿って確認する
  - `npm run deploy -- staging`。invalidation `I2MLYRM75NVGY9HSPM2WQJF4S0` が Completed。
  - https://diary.dev.apkas.net で、トップ・年別・月別・日別・月日ページが 200、下書きの `/2025/12/24` と
    `/2025/12` が 404。
  - 導線: `/2026/08/01`（最新）は古い側だけ、`/2023/01/01`（最古）は新しい側だけ。`/2026/01/01` は
    `←` が `/2026/06/07`、`→` が `/2025/11/30` で、年をまたぎつつ下書きの 12月24日 を飛ばしている。
  - `/2024` はエントリが 09/29→02/20 の降順、月への導線が 9月→8月→7月→2月。
  - トップ・年別・日別・月日ページのいずれにも `2025-12-24` の文字列は現れない。
