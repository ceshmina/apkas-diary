---
import Base from '../layouts/Base.astro'

export const prerender = false

// middleware を通っている以上ここに未認証は来ない。型のために取り出す。
const session = Astro.locals.session
---

<Base title="apkas-diary 編集" email={session?.email}>
  <h1>準備中</h1>
  <p class="muted">エントリの一覧はこの後のタスクで作る。</p>
</Base>
