{
  "name": "apkas-diary-photo-resize",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "アップロードされた元写真から配信用の派生画像を作る Lambda。",
  "//": "ルートの package.json とは独立させている。sharp は Linux 用の native binary を持ち、サイト生成の依存に混ぜたくないため。",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.1106.0",
    "@aws-sdk/client-dynamodb": "^3.1106.0",
    "@aws-sdk/client-s3": "^3.1106.0",
    "@aws-sdk/lib-dynamodb": "^3.1106.0",
    "exif-reader": "^2.0.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  }
}
