{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "//": "lambda/ を外すのは、そこが Node の Lambda で独自の tsconfig と依存（sharp の型）を持つため。ここに含めるとサイト生成の型検査が lambda/photo-resize/node_modules を要求するようになる。検査は scripts/check-lambda.sh が別に行う。",
  "exclude": ["dist", "node_modules", "export", "lambda"],
  "compilerOptions": {
    "types": ["node"],
    "strictNullChecks": true,
    "noUncheckedIndexedAccess": true
  }
}
